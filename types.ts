import { LucideIcon } from 'lucide-react';

export interface Differential {
  icon: LucideIcon;
  title: string;
  description: string;
}

export interface ProcessStep {
  title: string;
  desc: string;
}